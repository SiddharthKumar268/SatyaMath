{
  "name": "blockchain-evoting-ganache",
  "version": "1.0.0",
  "description": "Blockchain-based E-Voting System using Ganache",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run --network localhost scripts/deploy.js",
    "test": "hardhat test"
  },
  "keywords": ["blockchain", "voting", "ganache", "ethereum", "solidity"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "ethers": "^6.9.0"
  }
}